int main() {
    int x;
    int y;
    int z;
    z = x + 4 *23;
    printInt(x);
    printStr("+");
    printInt(y);
    printStr(" = ");
    printInt(z);
    if (z<y && x<y){
        z = x + 4 *23;
    }
    
}
